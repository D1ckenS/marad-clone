{
  "name": "@marad-clone/proto",
  "version": "0.0.0",
  "private": true,
  "description": "Protobuf schemas for sync wire protocol. Source of truth for cross-language types (TS via ts-proto, Dart via protoc_plugin)."
}
