{
  "name": "@fleetops/desktop-vessel",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "tsc -p tsconfig.build.json && electron .",
    "build": "tsc -p tsconfig.build.json",
    "prepare-bundle": "node scripts/prepare-bundle.mjs",
    "dist": "pnpm run build && pnpm run prepare-bundle && electron-builder",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@electron/rebuild": "3.7.1",
    "electron": "30.5.1",
    "electron-builder": "24.13.3"
  }
}
